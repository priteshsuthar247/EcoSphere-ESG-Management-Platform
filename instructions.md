1. do not hard code any sample/dummy data ..... make database connections and retrieve data from db only ..... no hard coded data or sample..... everything should be dynamic ... 
2. if using any api or any kind of secrets then use .env file , do not hard code secrets into the code 
3. logging for error handling and debug 
4. Design proper, consistent API endpoints for every use case
5. Make everything modular & expandable
6. Proper folder structure is non-negotiable
eg. /src/routes, /services, /models, /middleware, /utils, /config, /tests
7. follow the ui of the website according to the design.md file , follow that like life depends on it 
8. Make a colour.md file, and all the UI colours should be configurable from that single file ... So that we can change the colour combo of the website easily from that colour.md file.9.  make the website responsive, mobile friendly , tablet friendly simultaneously as u make the website for desktop view. 
10. security is top most priority, so at the end of each new tasks/feature added , check the security of the complete project. also specifically check the RBAC(role based access control), also make sure that this website does not contain any common vulnerabilities like OWASP top 10 or any other vulnerabilities ......
11. make sure the the product flow/data flow/any feature flow is complete and proper ... and also check this flow system is working or not after each new tasks/feature added 
12. and make sure that the project is deployment ready ..... this is not a dummy project , this is pre- deployment stage.... so at regular interval check if any bugs are present, if present then resolve it ......
13. if u don not understand anything then ask question to the user ..... do not assume anything ..... the ideation/planning is our(user) task, u(ai) just need to follow our idea and implement the code accordingly, if any ambiguity/problem/issue/question then ask question to the user , do not assume anything1. do not hard code any sample/dummy data ..... make database connections and retrieve data from db only ..... no hard coded data or sample..... everything should be dynamic ... 
2. if using any api or any kind of secrets then use .env file , do not hard code secrets into the code 
3. logging for error handling and debug 
4. Design proper, consistent API endpoints for every use case
5. Make everything modular & expandable
6. Proper folder structure is non-negotiable
eg. /src/routes, /services, /models, /middleware, /utils, /config, /tests
7. follow the ui of the website according to the design.md file , follow that like life depends on it 
8. Make a colour.md file, and all the UI colours should be configurable from that single file ... So that we can change the colour combo of the website easily from that colour.md file.9.  make the website responsive, mobile friendly , tablet friendly simultaneously as u make the website for desktop view. 
10. security is top most priority, so at the end of each new tasks/feature added , check the security of the complete project. also specifically check the RBAC(role based access control), also make sure that this website does not contain any common vulnerabilities like OWASP top 10 or any other vulnerabilities ......
11. make sure the the product flow/data flow/any feature flow is complete and proper ... and also check this flow system is working or not after each new tasks/feature added 
12. and make sure that the project is deployment ready ..... this is not a dummy project , this is pre- deployment stage.... so at regular interval check if any bugs are present, if present then resolve it ......
13. if u don not understand anything then ask question to the user ..... do not assume anything ..... the ideation/planning is our(user) task, u(ai) just need to follow our idea and implement the code accordingly, if any ambiguity/problem/issue/question then ask question to the user , do not assume anything